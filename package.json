{
  "name": "timelog",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "express": "^4.16.3",
    "react-scripts": "^1.1.5"
  },
  "dependencies": {
    "left-pad": "^1.1.3",
    "moment": "^2.22.2",
    "react": "^15.3.2",
    "react-dom": "^15.3.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "publish": "npm run build && pm2 restart timelog",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
