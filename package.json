{
  "name": "timelog",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "express": "^4.14.0",
    "react-scripts": "0.7.0"
  },
  "dependencies": {
    "left-pad": "^1.1.3",
    "moment": "^2.16.0",
    "react": "^15.3.2",
    "react-dom": "^15.3.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "rebuild": "npm run build && pm2 restart timelog",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
